import { HeroVariantApple } from "@/components/hero/HeroVariantApple";

/** Default home hero — Apple minimal + CSS phone mockup. */
export function HomeHero() {
  return <HeroVariantApple headingLevel="h1" />;
}
